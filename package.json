{
  "name": "test-set-release-mapper-tests",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Test infrastructure for the Test Set Release Mapper SpiraApp",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^3.1.4",
    "fast-check": "^4.1.1"
  }
}
